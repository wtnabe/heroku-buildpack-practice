#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

SCRIPT=$1/.profile.d/fluentd.sh

gem install fluentd -N && cat <<EOF > $SCRIPT
#! /bin/sh

fluentd help
EOF

if [ -e $SCRIPT ]; then
    chmod a+x $SCRIPT
fi
