#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

SCRIPT=$1/.profile.d/fluentd.sh

gem install fluentd -N && cat <<EOF > $SCRIPT
#! /bin/sh

erb fluentd/fluent.conf.erb > fluentd/fluent.conf
$HOME/vendor/bundle/bin/fluentd -c fluentd/fluent.conf
EOF

if [ -e $SCRIPT ]; then
    chmod a+x $SCRIPT
else
    echo 'fluentd not installed'
fi
